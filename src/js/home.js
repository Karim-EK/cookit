const container = document.getElementById("dynamic-container");
const ingrBtn = document.getElementById("ingr-btn"); 
const prepBtn = document.getElementById("prep-btn");

ingrBtn.addEventListener("click", () => showIngredients());
prepBtn.addEventListener("click", () => showPreparation());

function showIngredients() {
    
}